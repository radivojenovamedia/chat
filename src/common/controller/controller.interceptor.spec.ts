import { ControllerInterInterceptor } from './controller.interceptor';

describe('ControllerInterInterceptor', () => {
  it('should be defined', () => {
    // expect(new ControllerInterInterceptor()).toBeDefined();
  });
});
